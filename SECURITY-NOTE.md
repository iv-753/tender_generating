# 保密说明

本项目 V1 仅限 `localhost` 内部演示。模型仅由绑定 `127.0.0.1` 的 Node 服务读取，浏览器和静态构建产物均不包含模型文件。

- 原始模型文件及任何包含该模型的构建产物，不得提交、上传、部署或外发。
- 本机服务只暴露 JSON 测算接口和 `dist` 静态文件，不提供 Excel 下载路由。
- 正式对外前，必须将 122 项计算规则迁移为可审查的代码或 JSON。
- 对外发布前，必须检查源码、网络请求、source map 和全部构建产物，确认其中不含原始模型或可恢复的模型内容。
