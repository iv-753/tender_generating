# 保密说明

本项目支持本机演示和 Vercel 部署。浏览器、服务器运行链路、Git 仓库及静态构建产物均不包含成本模型文件。

- 原始模型文件不得提交 Git、上传 Blob 或打进构建产物；线上测算直接使用已迁移并经过对照测试的静态规则。
- 本机服务只暴露 JSON 测算接口和 `dist` 静态文件，不提供 Excel 下载路由。
- 线上生成的 PPT 和标书保存在私有 Blob，页面仅获得 24 小时有效的单文件下载链接。
- `QWEN_API_KEY` 等密钥只允许配置在 Vercel 环境变量中，不得写入源码或提交 Git。
- 122 项计算规则已迁移为可审查的静态配置，原表仅限本地迁移对照测试使用。
- 对外发布前，必须检查源码、网络请求、source map 和全部构建产物，确认其中不含原始模型或可恢复的模型内容。
